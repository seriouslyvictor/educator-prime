# Optional API origin override for local development or split deployments.
# Leave blank when the frontend is served by the FastAPI backend.
VITE_API_BASE_URL=

# No frontend Sentry DSN is required by this plan. The Admin observability UI
# uses VITE_API_BASE_URL and server-side CD_ADMIN_EMAILS authorization.
