# Implementation Plans

Generated by the improve skill on 2026-06-29. The active `plans/` folder was
empty, but `archive/` already contains completed numbered plans through 027.
New plans therefore continue at 028. Execute in the order below unless a
dependency says otherwise. Each executor must read the target plan fully before
starting, honor STOP conditions, and update the status row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 028 | Split the monolithic grading backend test module | P1 | M | none | DONE |
| 029 | Extract grading router services and shared SSE runner | P1 | M | 028 | TODO |
| 030 | Extract a testable grading EventSource client and reducer | P1 | M | none | DONE |
| 031 | Split the web API client into transport, cache, and endpoint modules | P2 | M | 030 recommended | TODO |
| 032 | Split Google provider implementation from mocks, credentials, and cache helpers | P2 | M | 028 | TODO |
| 033 | Split the grading drafting pipeline into policy, submission, outlier, and job services | P2 | L | 028, 029 | TODO |
| 034 | Split the shared grader CSS module by screen/component | P3 | M | archive/008, archive/009 completed | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- 028 should land first because the highest-risk backend refactors need smaller,
  behavior-focused tests before code moves.
- 029 should precede 033 because `drafting.py` and the grading router are coupled
  through streaming and job lifecycle behavior; thinning the router makes the
  drafting split easier to review.
- 030 is independent of backend work and can run in parallel with 028/029.
- 031 is safer after 030 because the API client refactor will affect grading
  workflow calls used by `useGradingJob`.
- 034 explicitly builds on archived plans 008 and 009, which decomposed grader
  screens while intentionally leaving `Grader.module.css` untouched.

## Existing archive considered

- `archive/005-decompose-app-tsx.md` already extracted the major frontend hooks
  from `App.tsx`. Plan 030 targets the next layer inside `useGradingJob`, not a
  repeat of that work.
- `archive/008-decompose-grader-review.md` and
  `archive/009-decompose-grader-setup-queue.md` already decomposed grader screen
  components. Plan 034 only addresses the remaining shared CSS file.
- `archive/024-isolate-outlier-review-failure.md`,
  `archive/025-preview-retry-e2e-coverage.md`,
  `archive/026-lazy-activity-grade-summaries.md`, and
  `archive/027-real-file-test-corpus.md` cover recent grading behavior,
  preview coverage, activity summary performance, and real-file corpus work.
  New backend plans must preserve those invariants.

## Findings considered and rejected

- Re-doing `App.tsx` decomposition: rejected because archive plan 005 already
  completed the hook extraction and recorded the remaining circular refs as a
  deliberate compromise.
- Re-doing `GraderReview`, `GraderSetup`, or `GraderQueue` decomposition:
  rejected because archive plans 008 and 009 already cover those component moves.
- Making lint blocking in CI as a standalone plan: rejected for now because the
  maintenance payoff is lower than the separation-of-concerns work above.
